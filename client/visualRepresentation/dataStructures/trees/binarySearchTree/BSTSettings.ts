import Area from "../../../common/areas/Area";

class BSTSettings {
    static canvasArea: Area = new Area(1000, 600);

    static codeControlArea: Area = new Area(450, 600);
}

export default BSTSettings;